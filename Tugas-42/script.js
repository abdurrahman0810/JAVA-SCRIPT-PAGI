// !! contoh deklarasi fungsi

function calculateArea(length, width) {
  return length * width;
}

// ?? jika menggunakan exprection function

let  Calculator  = function (lebar, tinggi) {
  return lebar * tinggi;
};

// ?? jika menggunakan Arrow Function

let calculateArea = (length, width) => length * width;
